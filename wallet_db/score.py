"""
Phase 4 — Scoring engine (stub).
Full implementation coming after Phase 2b/2c are live.
"""
import logging
logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")
log = logging.getLogger(__name__)

if __name__ == "__main__":
    log.info("Scoring engine — not yet implemented, skipping.")
