"""
Phase 6 — Tiering job (stub).
Full implementation coming after Phase 4 scoring is live.
"""
import logging
logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")
log = logging.getLogger(__name__)

if __name__ == "__main__":
    log.info("Tiering job — not yet implemented, skipping.")
